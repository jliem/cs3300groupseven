package colab.common;

public class DebugManager {

    /** Flag for printing exceptions produced when a window is closing, which
     * can probably be disregarded.
     */
    public static final boolean EXIT_EXCEPTIONS = true;
}
